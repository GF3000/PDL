let counter int; //declara counter
let sentence string;
sentence = 'This is a simple program.';
print(sentence);
comment('This is a comment and will be ignored by the lexer.');
for (counter = 1; counter < 8; --counter) {
  print('Iteration #' + counter);
}